#include "main.h"

/**
 * _abs - prints absolute value of integers
 * @n: integer whose absolute value is printed
 *
 * return: absolute value of integer
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
