#include <stdio.h>

int main() {
	const char *str = "Hello, World!"
		for (int i = 0; str[i] != '\0'; i++) {
			        putchar(str[i]);
				    }
		    
		    putchar('\n'); // Print a newline character at the end
		        return 0;
}


