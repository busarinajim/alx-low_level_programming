#include "main.h"

/**
 * _puts - prints string to stdout
 * @str: string to be printed
 *
 */

void _puts(char *str)
{
	int len = 0;

	while (*str != '\0')
	{
		_putchar('str');
		len++;
	}
	_putchar('\n');
}
