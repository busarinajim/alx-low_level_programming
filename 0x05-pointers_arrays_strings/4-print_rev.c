#include "main.h"

/**
 * print_rev - prints string in reverse order
 * @s: string to be printed
 *
 */

void print_rev(char *s)
{
	int len;

	for (len = '\0'; len = *s; len--)
	{
		_putchar('len');
	}
	_putchar('\n')
}
